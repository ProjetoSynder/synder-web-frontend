import { styled } from 'styled-components'
import { colors } from '../../styles/styles'

export const InputsLoginContainerDiv = styled.div`
  display: flex;
  flex-direction: column;
  div {
    margin-bottom: 22px;
  }
`
